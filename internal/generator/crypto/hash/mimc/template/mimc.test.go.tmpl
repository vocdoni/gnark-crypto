import "fmt"

func printError(err error) {
	if err != nil {
		fmt.Println("error:", err)
	}
}

func Example() {

	in := make([]byte, BlockSize)
	in[0] = 255
	hsh := NewMiMC()
	_, err := hsh.Write(in)
	printError(err)

	// Output:
	// error: invalid fr.Element encoding
}
